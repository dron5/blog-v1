import React from "react";
import classes from "./ArticlePreview.module.scss";

const ArticlePreview = () =>(
  <div className={classes.preview}>Hello, world! I`m an ArticlePreview)</div>
);

export default ArticlePreview;